version: v1
name: buf.build/$BUF_USER/grpc-gateway-swagger-buf
deps:
  - buf.build/googleapis/googleapis
  - buf.build/grpc-ecosystem/grpc-gateway
breaking:
  use:
    - FILE
lint:
  use:
    - DEFAULT
